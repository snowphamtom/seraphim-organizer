# SERAPHIM ORGANIZER — MASTERPIECE_PLUS

**Date:** 2026-09-20 CT (box ~2026-09-21 02:23 UTC)  
**Status:** FROZEN for Wizard SHOW-OFF densify pass. HTML edits stopped.  
**Sync:** `docs/index.html` ≡ `standalone.html` (`23172922a637b64b0c6fdb023f78b885`)  
**Syntax:** `node --check` on extracted `<script id="app">` — OK  
**Law unchanged:** `S_H = S_solv · R_H` fail-closed · admit→crystallize · refuse→release (soft) · φ/coherence observe-only · offline single-file

---

## What this pass already changed

### 1. Label halo + collision relief (LENS #1 — partial)
- `neonLabel` draws a **2–3px black plate** + double soft black stroke, then fill — labels sit above bloom.
- Funnel/vertex labels pulled **~10px outward** along radial from center.
- Primary verbs parked **outside dense core**: `CRYSTALLIZE` at `cx±s*0.72` (left lime, right magenta `#ff6ec7`).
- Secondary labels (`UNSORTED / INTAKE`, `C/I %`) pushed farther out (`0.95` / `0.78` radii).

### 2. Toast → header chip (LENS #2)
- Removed center `#toast` overlay (was covering hex / colliding with log on mobile).
- Added `#introChip` under header metrics: **"Crystallize = kept · Release = outside seal"** + Got it.
- `localStorage` key `seraphim_toast_v1` dismisses forever (same key as before).
- Chip never covers hex by design (fixed under top bar).

### 3. Instrument contrast (LENS #3)
- Log: titles `#e8eef8`; CRYSTALLIZE stamp lime `#b8ff70`; RELEASE stamp magenta `#ff6ec7`; panel bg alpha ~0.72.
- `#sub` legend ~**12px** with lime/magenta swatches + `S_H` line.
- Cheat sheet enlarged; readable kbd + swatch legend; bigger close hit target.
- Header metric tooltips for n / crystallized / released / S_H.

### 4. Bipolar lava scaffold (LENS #4 — starter, not full metaballs)
- `hueClampAdmit` → **140–190** (lime/cyan band).
- `hueClampRelease` → **300–30** wrap (magenta→orange).
- Voxels: hard **left** hemisphere bias (`bx − 0.18`); release mesh/parts hard **right** (`+0.38`) with grown particle count.
- Release draw uses hue-clamped lava (halo + lighter cores); non-focused dimmed.
- Trail nodes use same clamps + stronger sideBias.
- **Not done here (Wizard owns):** true metaball fields, parabolic funnel meshes, 4–8× starfield blizzard, thick spectrum hex.

### 5. Chrome sparse / orbit dense (LENS #5 + mobile stack)
- Intake collapsed: **Drop · Sort · Clear** + demo select + **3-line peek** textarea.
- JSON / CSV / ✦ / ◫ / ♪ / ? → **`···` overflow** (`#morePanel`).
- Hex scale raised: `s = min(W,H) * 0.60 * zoom` (was 0.40); mobile orbit center `cy ≈ 0.38H` to leave dock space.
- **`@media (max-width:900px)`:** intake + log **fixed to bottom stack** under hex — never upper-left overlay; introChip stays header-only; navhint/sub hidden; log unmasked, capped height.

### Other
- `docs/favicon.ico` present + linked (`sizes="any"`); SVG data-URI retained.
- User-visible vaporize/destroy/ash copy removed; internal `nodes` array name kept.
- Offline zip lightly refreshed (HTML + favicons); large refs/`*.gif` not re-encoded.
- Controls retained: drag/pan/zoom/WASD/Space/F/1-2-3/?/✦/◫/♪/demo stories/auto Mixed Drift.

---

## Files touched
| Path | Note |
|------|------|
| `docs/index.html` | Live Pages entry (authoritative) |
| `standalone.html` | Byte-identical to docs |
| `docs/favicon.ico` | New / kept |
| `favicon.ico` | Root copy for local standalone |
| `SeraphimOrganizer-offline.zip` | HTML + favicon updated |
| `MASTERPIECE_PLUS.md` | This file |

**Not pushed.** Leave commit to Wizard / Taylor.

---

## Remaining risks / handoff to Wizard densify

1. **Orbit density still below LENS masterpiece bar** — intentionally frozen. Wizard: parabolic funnel meshes, 4–8× field blizzard (panel-clipped only — no white flood), bipolar **metaball** lime/cyan vs magenta/orange, thick spectrum hex stroke.
2. **Label vs densify race** — after Wizard densifies, re-verify CRYSTALLIZE/RELEASE plate still clears metaballs; may need another outward nudge.
3. **Mobile (~390)** — stack CSS is mandatory; densify must not reintroduce upper-left intake or center toast. Re-QA with `vault/seraphim-qa-2026-09-20/wizard-optic-mobile.png` pattern.
4. **Hex at 0.60** may clip against bottom instrument dock on short phones — densify should keep `cy` mobile bias.
5. **Zip** optional polish: include this `MASTERPIECE_PLUS.md` on next refresh.
6. **Stranger test** (first 5s): intro chip + bipolar sides + S_H chip in header — should pass after densify if chip copy stays plain.

---

## Freeze note
SHOW-OFF MODE: **no further HTML edits from this agent.** Wizard owns the canvas densify pass.
