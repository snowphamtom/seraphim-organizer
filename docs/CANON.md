# Seraphim Organizer — canon

- **Centerpiece:** Taylor hex-iridescent GIF (`hexIridescentBg` / purple-ink honeycomb on black) full-bleed — NOT keyed-star / cyber-nature / lattice / gold / gift-stack as hero
- **Overlay:** keyed purple-ink dodeca wire (`keyedDodecaWireOverlay` / `dodeca-wire-keyed.webp|.png`) hub-locked on the seal/dodeca (`--seal-x/--seal-y`), `mix-blend-mode:screen`, sized to the dodeca — not full-bleed
- **Core seed:** keyed purple-ink Seed-of-Life (`clientSeedInDodeca` / `seed-in-dodeca-keyed.webp|.png`) CENTERED INSIDE the wire at the same hub, screen/lighter, ~0.48 wire scale — not full-bleed hero
- **Proportional figures:** `clientProportionalFigures|sharedSealMetrics` — ALL figures share ONE center (`--seal-x/--seal-y` + `syncGyroSharedCenter`), ONE size family from `--seal-r` (seed < wire ≤ dodeca < network < field), ONE angle/orientation (shared `--gyro-*` / φ rigid stack); no independent random sizes or spins
- **Purple-only site:** `purpleOnlySite|allPurpleHues` — ENTIRE site hues of purple only (lilac→violet→deep purple→near-black). NO gold, cyan, magenta-as-hot-pink, orange, green chrome. Keyed assets regenerated as purple ink on transparent; CSS filters reinforce
- **Purple mono ramp:** `purpleMonoRamp|purpleWhiteBlack` — color format is purple shades from white–black; desk dark purple glass
- **No static straight lines:** `noStaticStraightLines|allLinesInMotion` — field/engine lines ride omni φ-gyro / purple-sphere TEMPO, are curved/elliptical/organic, or are hidden/demoted; reduced-motion prefers still curves / soft shapes over frozen straight grids; desk UI chrome OK for usability
- **Motion sync purple spheres:** sphere field (`gold-dotfield-keyed` purple ink, 90×50ms = 4.5s) is MASTER clock; dodeca/wire/seed/network/gyro lock to `--gold-T` / `--sphere-T` / φ family (Tphi2=4.5s); no competing clocks
- **Showpiece punch:** `impressShowpiece|showOffPass` — purple node GIF fully visible+moving; smooth φ omni gyro rigid stack; depth theater field→wire→seed→desk as one organism; proportional hub locked; crisp sort-desk glass; idle demo pauses on type; reduced-motion → stills
- **Depth perspective:** `depthPerspectiveHarmony|perspectiveDepthPass` — FOV theater: far hex+sphere-field cooler/dimmer/smaller foreshortened; mid network+wire midtones; near seed+seal bones largest/brightest; φ TEMPO; reduced-motion stills
- **Purple moving nodes:** animated `gold-dotfield-keyed.webp` (90-frame, purple ink) primary moving node field (screen/lighter; still only for reduced-motion) + `gold-network-keyed` purple-ink continuous φ spin/pulse on `.omniGyroRig` hub
- **Sync:** `clientOmniGyroSync|dodecaGyroAgreed|clientDodecaSync` — ONE shared hub (`--seal-x/--seal-y` + `syncGyroSharedCenter`); wire+seed+network in `.omniGyroRig` (+ sphere field) share cam YPR (`--gyro-yaw/--gyro-pitch/--gyro-roll`) with canvas dodeca — one rigid stack; ONE φ TEMPO (`syncTempoClock` / `--field-T*` locked to `--gold-T`); no second dodeca off-hub; no overlay drift
- **Palette:** near-black purple void `#0a0014` + lilac/violet/deep-purple only (`purpleOnlySite|allPurpleHues`); no gold/cyan/orange/green accents
- **Bg:** purple void + `media/hex-iridescent-bg.webp` (anim, purple ink) / `hex-iridescent-bg.gif` fallback / `hex-iridescent-bg-still.webp` (reduced-motion); soft blur so hex edges do not read as dead straight strokes
- **Chrome:** left `#dataRail`, bottom `#specBar` (purple); desk labels **Disorganized / Organized / Aside**
- **Depth ladder (back→front):** void → hex-iridescent (far) → purple sphere field (far foreshortened) → canvas/procedural dodeca → keyed wire + purple network (mid) → keyed seed core (near/bright) → desk (dark purple glass front)
- **Seal:** thick readable curved Magpie bones (lilac↔violet); shared hub `--seal-x/--seal-y`; φ TEMPO locked to purple spheres
- **Motion:** purple sphere field MASTER (`--gold-T:4.5s` / `--sphere-T`); one φ family (`--field-T*` / gyro `2π/GOLD_T·φ^k`); reduced-motion → stills / soft curves / no tumble; idle demo pauses on type/focus
- **Fences:** no crystallize / release / dump / S_H in user-visible strings; no XYZ room grid; no white-paper void; no lattice-as-hero; no stationary straight field lines; prior heroes opacity 0 / display none; no gold/cyan/hot-pink/orange/green chrome
- **Live tip:** `?v=<shortsha>` — tip: purpleOnlySite|allPurpleHues (entire site purple hue family; purple-ink assets; sphere MASTER clock)
