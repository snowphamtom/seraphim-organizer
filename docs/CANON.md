# Seraphim Organizer — canon

- **Centerpiece:** Taylor hex-iridescent GIF (`hexIridescentBg` / purple-ink honeycomb on black) full-bleed — NOT keyed-star / cyber-nature / lattice / gold / gift-stack as hero
- **Overlay:** keyed purple-ink dodeca wire (`keyedDodecaWireOverlay` / `dodeca-wire-keyed.webp|.png`) hub-locked on the seal/dodeca (`--seal-x/--seal-y`), `mix-blend-mode:screen`, sized to the dodeca — not full-bleed
- **Core seed:** Seed-of-Life as INTERIOR CORE of neon dodeca (`seedOfLifeDodecaCore|seedInsideDodeca` / keyed `seed-in-dodeca-keyed.webp|.png` + canvas procedural) — same hub (`--seal-x/--seal-y`); clearly smaller than dodeca (~0.40); depth-sorted INSIDE cage (back edges → seed → front edges); rides SAME φ-gyro as tumbling dodeca (rigid with core); still dodeca also carries still-core; screen/lighter; purple/black/white — not full-bleed hero
- **Proportional figures:** `clientProportionalFigures|sharedSealMetrics` — ALL figures share ONE center (`--seal-x/--seal-y` + `syncGyroSharedCenter`), ONE size family from `--seal-r` (seed < wire ≤ dodeca < network < field), ONE angle/orientation (shared `--gyro-*` / φ rigid stack); no independent random sizes or spins
- **Palette law:** `refinePolishShow|beneficialVariations|purpleOnlySite` — PURPLE + BLACK + WHITE only (lilac/violet = purple shades). Soft ramp near-white lilac → deep void purple; black void; white highlights. NO gold, cyan, hot-magenta, orange, green chrome
- **Light + shadow depth:** `lightShadowDepth|angledLightDepth` — explicit upper-left + hub-key lights; soft purple-tinted cast shadows; specular white highlights; angled shading on still+tumble dodeca, tethers, sphere field (near/far via light, not size alone)
- **No stagnant lines (Taylor):** `noStagnantLines|allLinesInMotion` — every line moves with φ/gyro, is curved, or is hidden; tethers may ride tumbling dodeca; no static straight grids
- **Bg symbiotic layers:** `bgSymbioticLayers` — everything EXCEPT seed-of-life + neon dodeca harmonizes with the purple void (blend/opacity/color); seed + neon dodeca stay clear heroes
- **Hero cull + fog + breath:** `heroCullFogBreath|taylorJudgmentPolish` — cull competing figures; soft void fog/vignette; seed subtle φ-breath on sphere MASTER clock; thinner softer tethers (still attached); quieter glass desk; gentler idle (pause-on-type kept)
- **Dual dodeca tethers:** `clientDualDodecaTether|clientPurpleReformat` — one STILL (fixed YPR) + one tumbling (omni φ-gyro); visible purple corner-to-corner tethers still[i]→moving[i] stay attached every frame; one hub
- **Purple mono ramp:** `purpleMonoRamp|purpleWhiteBlack` — desk dark purple/black glass; white-lilac labels
- **No static straight lines:** `noStaticStraightLines|allLinesInMotion` — field/engine lines ride omni φ-gyro / purple-sphere TEMPO, are curved/elliptical/organic, or are hidden/demoted; reduced-motion prefers still curves / soft shapes over frozen straight grids; desk UI chrome OK for usability; moving tethers OK
- **Motion sync purple spheres:** sphere field (`gold-dotfield-keyed` purple ink, 90×50ms = 4.5s) is MASTER clock; dodeca/wire/seed/network/gyro lock to `--gold-T` / `--sphere-T` / φ family (Tphi2=4.5s); no competing clocks
- **Showpiece / polish:** `impressShowpiece|refinePolishShow` — readable sphere nodes; soft hub vignette (anti-blotch); fewer competing layers; crisp Disorganized/Organized/Aside desk; idle demo pauses on type; reduced-motion → stills
- **Depth perspective:** `depthPerspectiveHarmony|perspectiveDepthPass` — FOV theater: far hex+sphere-field cooler/dimmer/smaller foreshortened; mid network+wire midtones; near seed+seal bones largest/brightest; φ TEMPO; reduced-motion stills
- **Sync:** `clientOmniGyroSync|dodecaGyroAgreed|clientDodecaSync` — ONE shared hub; wire+seed+network in `.omniGyroRig` share cam YPR with canvas dodeca — one rigid stack; ONE φ TEMPO
- **Bg:** black/purple void + `media/hex-iridescent-bg.webp` (anim) / still for reduced-motion; soft blur so hex edges do not read as dead straight strokes
- **Chrome:** left `#dataRail`, bottom `#specBar` (purple); desk labels **Disorganized / Organized / Aside**
- **Depth ladder (back→front):** void → hex-iridescent (far) → purple sphere field → still dodeca + still seed-core → tethers → tumbling dodeca back edges → seed-of-life CORE → tumbling front edges + keyed wire (cage) → desk (front)
- **Seal:** thick readable curved Magpie bones (lilac↔violet↔white); shared hub; φ TEMPO locked to purple spheres; no blotchy hub disks
- **Fences:** no crystallize / release / dump / S_H in user-visible strings; no XYZ room grid; no white-paper void; no lattice-as-hero; no stationary straight field lines; no gold/cyan/hot-pink/orange/green chrome
- **Live tip:** `?v=<shortsha>` — tip: `seedOfLifeDodecaCore|seedInsideDodeca` (seed-of-life INSIDE neon dodeca as CORE; same hub; smaller; depth-sorted interior; same gyro as tumble; still core on still dodeca; purple/black/white; no stagnant lines; bg symbiotic; light/shadow; dual tethers; heroes = neon dodeca + seed-as-core; prior polish kept)
